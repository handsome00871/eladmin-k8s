apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: yu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: maweika/mysql:5.7
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: eladmin
          ports:
            - containerPort: 3306

